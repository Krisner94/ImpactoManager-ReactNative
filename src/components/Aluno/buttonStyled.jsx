import styled from "styled-components/native";
import { Button as PaperButton } from 'react-native-paper';

export const ButtonStyled = styled(PaperButton)`
    display: flex;
    justify-items: end;
    margin: 10px;
`
